import type { Metadata } from "next";
export const metadata:Metadata={title:"Reservierung verwalten",robots:{index:false,follow:false}};
export default function ManageReservationLayout({children}:{children:React.ReactNode}){return children}
